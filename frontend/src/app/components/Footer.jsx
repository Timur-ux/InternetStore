import React from "react";
import style from "../../style";

const Footer = (props) => {
  return <div style={{...style.footer, ...style.baseBackground}}>Some contact info placed here</div>;
};

export default Footer;
